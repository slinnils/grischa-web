---

---

<section id="contact">
  <div class="form-container fade-in card">
    <div>
      <h2 class="section-title">Lassen Sie uns Ihr Projekt besprechen</h2>
    </div>
    <form action="https://api.web3forms.com/submit" method="POST">
      <input
        type="hidden"
        name="access_key"
        value={import.meta.env.PUBLIC_WEB3FORMS_KEY}
      />
      <label class="title" for="name">Name</label>
      <input type="text" name="name" required />

      <label class="title" for="email">E-Mail</label>
      <input type="email" name="email" required />

      <label class="title" for="message">Ihre Nachricht</label>
      <textarea name="message" required></textarea>
      <button class="btn-primary" type="submit">Senden</button>

      
      <span>Ich melde mich in der Regel innert 24 Stunden.</span>

      <input type="checkbox" name="botcheck" style="display: none;" />
    </form>
  </div>

  <div class="info card fade-in">
    <h3 class="section-title">Meine Kontaktangaben:</h3>
    <p>Nils Imbach</p>
    <a href="mailto:nils@grischa-web.ch">nils@grischa-web.ch</a>
    <a href="tel:+41799689805">+41 79 968 98 05</a>
    <p>Standort: Chur, Graubünden</p>
    <hr />
  </div>
</section>

<style>
  section {
    display: flex;
    gap: 1.8rem;
    width: 100%;
    max-width: 1200px;
    justify-content: center;
    margin: auto;
  }

  @media (max-width: 769px) {
    section {
      flex-direction: column;
      align-items: center;
    }
  }

  .form-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    text-align: center;
    width: 100%;
    max-width: 536px;
    margin: 0 auto;
  }

  label {
    color: var(--color-gw-text);
    font-size: clamp(0.9rem, 3vw, 1.2rem);
    font-weight: 900;
    align-self: flex-start;
    text-transform: uppercase;
  }

  form {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-size: clamp(1rem, 10vw, 2rem);
  }

  button {
    font-size: 1.2rem;
    cursor: pointer;
  }


  input,
  textarea {
    margin-bottom: 1.6rem;
    width: 100%;
    max-width: 600px;
    font-size: 1rem;
    color: var(--color-gw-text-bright);
    outline: none;
  }

  input{
    border-bottom: 1px solid var(--color-gw-text-mute);
    padding: 0.3rem 0 0.3rem 0.5rem;
  }

  input:focus{
    border-bottom: 1px solid var(--color-gw-accent);
  }

  textarea{
    border: 1px solid var(--color-gw-text-mute);
    border-radius: 15px;
    padding: 0.5rem 1rem;
    margin-top: 0.8rem;
    height: 200px;
  }

  textarea:focus{
    border: 1px solid var(--color-gw-accent);
  }

  span{
    font-size: 0.8rem;
    color: var(--color-gw-text-mute);
    margin-top: 1.5rem;
  }

  .info{
    width: 100%;
    text-align: center;
    font-size: clamp(1rem, 2.5vw, 1.5rem);
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    box-shadow: 0 5px 10px rgba(0, 0, 0, 0.158);

    transition-delay: 0.25s;
  }

  hr{
    display: none;
    margin-top: 2rem;
    width: 50%;
  }

  @media(min-width: 769px){
    hr{
      display: block;
    }
  }
</style>
